---
# Source: helm for kubernetes/templates/ingress.yaml
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: bootop                  # Ingress name
  labels:
    app: bootop
spec:
  ingressClassName: nginx   # Optional ingressClass
  rules:
    - host: "bootop.smartops.space"
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: bootop               # Links to Service name dynamically
                port:
                  number: 8080  # Links to service port dynamically
